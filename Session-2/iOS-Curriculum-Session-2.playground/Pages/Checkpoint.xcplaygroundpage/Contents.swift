//: [Previous](@previous)

import Foundation

/*:
 # Checkpoint
 
 - Your input is this:
 */
let luckyNumbers = [7, 4, 38, 21, 16, 15, 12, 33, 31, 49]

/*:
 - Your job is to:
    - (Filter) out any numbers that are even
    - (Sort) the array in ascending order
    - (Map) them to strings in the format if value == 7 “(element) is a lucky number” else “(element) isn't a lucky number”
    - Print the resulting array, one item per line
 
*/

//: [Next](@next)
